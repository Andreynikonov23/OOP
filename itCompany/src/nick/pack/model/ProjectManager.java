package nick.pack.model;

public class ProjectManager extends IT {
    public ProjectManager(String name, int age, int salary, String project){
        setData(name, age, salary, project);
    }
}
